config:
  target: "http://localhost:3000/api"
  phases:
    - duration: 60 # virtual users will be generated for 60 seconds
      arrivalRate: 1000 # 1000 virtual users sent to the endpoints per second

